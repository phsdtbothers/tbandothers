#' @import utils
# declare global variables (for no comments during devtools::check())
utils::globalVariables(c(
  ".", "case_id", "case_id_num", "cases", "dup_case_id", "dup_case_id_min",
  "dup_completeness", "dup_completeness_match", "dup_completeness_max", "dup_id",
  "dup_points", "dup_points_match", "dup_points_max", "provcity", "proxy_onset_date",
  "region", "words", "words_soundex", "words_soundex_in", "words_split"
))
